<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f2ffd8"
    android:orientation="vertical"
    tools:context=".scheme_actitvy">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:paddingTop="10dp"
        android:background="@android:color/white"
        android:elevation="6dp"
        app:title="Schemes"
        app:titleTextColor="@android:color/holo_green_dark" />

    <!-- Input for Date -->
    <EditText
        android:id="@+id/editTextdate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="200dp"
        android:layout_marginHorizontal="60dp"
        android:hint="Enter Date (e.g. 2025-11-03)"
        android:padding="12dp"
        android:textColor="#8E03A1"
        android:textColorHint="#8E03A1"
        android:background="@drawable/editetextfield"
        android:inputType="text"
        android:elevation="20dp" />

    <!-- Fetch Button -->
    <Button
        android:id="@+id/btnfetchscheme"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Fetch Scheme"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="100dp"
        android:backgroundTint="@android:color/holo_green_dark"
        android:textColor="@android:color/white"
        android:padding="16dp"
        android:layout_marginBottom="24dp"
        android:elevation="20dp" />

    <!-- Scrollable container for cards -->
    <ScrollView
        android:id="@+id/scrollSchemes"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:padding="12dp">

        <LinearLayout
            android:id="@+id/schemeContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp" />
    </ScrollView>

</LinearLayout>
