<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Firebase requires internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgriBandhu">
        <activity
            android:name=".crop_actitvty"
            android:exported="true" />
        <activity
            android:name=".post"
            android:exported="true" /> <!-- Splash or Auth activity (optional) -->
        <!-- LoginActivity is your launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Sign Up -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true" /> <!-- Main app features -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".Main_Account"
            android:exported="true" />
        <activity
            android:name=".scheme_actitvy"
            android:exported="true" />
        <activity
            android:name=".AIChatbot"
            android:exported="true" />
        <activity
            android:name=".Ai_chat"
            android:exported="true" />
        <activity
            android:name=".practical"
            android:exported="true" />
        <activity
            android:name=".weather_page"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
    </application>

</manifest>